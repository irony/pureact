{
  "name": "pureact",
  "version": "0.7.1",
  "author": "Christian Landgren",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/irony/pureact"
  },
  "main": "dist/bundle.js",
  "dependencies": {
    "vdom": "^0.0.22",
    "vtree": "^0.0.22"
  },
  "browserify": {
    "plugins": [],
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "react",
            "es2015"
          ]
        }
      ]
    ]
  },
  "devDependencies": {
    "babel-core": "^6.24.1",
    "babel-loader": "^7.0.0",
    "babel-preset-es2015": "^6.9.0",
    "babel-preset-react": "^6.11.1",
    "babelify": "^7.3.0",
    "oboy": "^1.2.0"
  },
  "scripts": {
    "test": "./node_modules/.bin/mocha tests/*",
    "watch": "./node_modules/.bin/mocha -w tests/*",
    "build": "./node_modules/.bin/browserify index.js --outfile dist/bundle.js",
    "prepublish": "npm test && npm run build"
  }
}
